[package]
name = "efi_main"
version = "0.1.0"
edition = "2024"

[dependencies]
uefi = { version = "0.38", features = ["alloc", "global_allocator"] }
uefi-raw = "0.15.0"

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
