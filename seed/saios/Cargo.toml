[package]
name = "saios"
version = "0.1.0"
edition = "2024"

[dependencies]
efi_main = { path = "../../boot/uefi/efi_main" }
hal = {path = "../../hal" }
spin = "0.10"

[profile.dev]
panic = "unwind"

[profile.release]
panic = "unwind"
