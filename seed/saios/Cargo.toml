[package]
name = "saios"
version = "0.1.0"
edition = "2024"

[dependencies]
efi_main = { path = "../../boot/uefi/efi_main" }
hal = {path = "../../hal" }
heapless = { version = "0.8", default-features = false }

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
